{
  "name": "rails-app",
  "version": "1.0.1",
  "description": "Rails boilerplate application",
  "engines": {
    "node": "7.8.0"
  },
  "scripts": {
    "setup": "gem install bundle && bundle install --without production && bundle update",
    "test": "rails test && heroku run rails test",
    "deploy": "git push heroku master && heroku run rake db:migrate && heroku open",
    "logs": "heroku logs"
  }
}
