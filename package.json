{
  "name": "rails-app",
  "version": "1.0.1",
  "description": "Rails boilerplate application",
  "engines": {
    "node": "7.8.0"
  },
  "scripts": {
    "deploy": "git push heroku master && heroku run rake db:migrate"
  }
}
